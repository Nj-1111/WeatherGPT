# Minimal — boots backend + trains all 3 models on CPU or Kaggle T4
# GRIB2 decoding (cfgrib/eccodes/xarray) in requirements-full.txt

fastapi==0.110.2
uvicorn[standard]==0.29.0
pydantic==2.7.4
python-multipart==0.0.9
httpx==0.27.2
python-dotenv==1.0.1

# ML — CPU torch by default; Kaggle notebook upgrades to cuda build automatically
# Flexible range, not a hard pin: torch==2.3.1 has no wheel for Python 3.12+
# (fails pip resolution entirely on newer systems, e.g. Python 3.14 -> only
# torch>=2.9.0 available). If you need an exact pin for reproducibility, pass
# --no-deps and pin torch yourself for your specific Python version.
torch>=2.3,<3
transformers==4.42.4
datasets==2.20.0
tokenizers==0.19.1
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.4
pyyaml==6.0.1
accelerate==0.31.0
lightgbm>=4.0
huggingface_hub>=0.24

# Geospatial + units (light — no eccodes here)
shapely==2.0.4
pint==0.24.3

# Utils
tenacity==8.5.0
orjson==3.10.7
