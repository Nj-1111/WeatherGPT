# Everything needed to run the live FastAPI backend and its test suite.
# The request path is fully deterministic and never loads an ML model
# (see GET /health -> models.bias_correction).
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-api.txt
#   uvicorn app.main:app --host 0.0.0.0 --port 8001
#   pytest -q

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
httpx==0.28.1

# Tests
pytest==9.1.1
