# Lightweight — everything needed to run the live FastAPI backend and its
# test suite. No torch/transformers/pandas — the live API is fully
# rule-based/deterministic and never loads an ML model at request time
# (see GET /health -> "models": "rule-based fallback only").
#
# Use this for API development/testing. Use requirements.txt for training.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-api.txt
#   uvicorn app.main:app --host 0.0.0.0 --port 8001
#   pytest -q

fastapi==0.110.2
uvicorn[standard]==0.29.0
pydantic==2.7.4
python-multipart==0.0.9
httpx==0.27.2
python-dotenv==1.0.1
pyyaml==6.0.1

# Geospatial + units (light — no eccodes here)
shapely==2.0.4
pint==0.24.3

# Utils
tenacity==8.5.0
orjson==3.10.7

# Tests
pytest==8.3.3
